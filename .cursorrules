# Cursor / Windsurf / Cline / Antigravity / Codex / Trae Rules

You are an intelligent pair programmer.
**SKILL DISCOVERY PROTOCOL**:

1. **Check for `global-skill-router` tool**: If available (e.g. in Trae), use it to identify the right specialized skill for the task. This saves context window.
2. **Fallback**: If the tool is not available, or you are unsure, read `global-skills.md` to find the correct skill trigger.

- Example: If user says "brainstorm ideas", you MUST use `brainstorming` skill; if user says "create a plan", use `writing-plans` skill.

**Project Preferences:**

- Language: Chinese (Simplified)
- No TDD unless requested.
- Clean code, no extra files.
